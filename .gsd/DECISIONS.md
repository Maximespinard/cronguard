# Decisions Register

<!-- Append-only. Never edit or remove existing rows.
     To reverse a decision, add a new row that supersedes it.
     Read this file at the start of any planning or research phase. -->

| # | When | Scope | Decision | Choice | Rationale | Revisable? | Made By |
|---|------|-------|----------|--------|-----------|------------|---------|
| D001 |  | architecture | Application architecture pattern | Vite SPA + separate Express API (no Next.js) | Owner's expertise is React + Vite + TanStack ecosystem. Separate API enables independent scaling and Railway deployment. No SSR needed for dashboard app. | no | human |
| D002 |  | architecture | Monorepo structure | apps/web, apps/api, packages/shared with npm workspaces | Shared types and utilities between frontend and backend. Single repo simplifies CI and deployment. npm workspaces over Turborepo to keep tooling minimal. | Yes | collaborative |
| D003 |  | infrastructure | Database provider | Neon Postgres (free tier) | Generous free tier (0.5GB storage, 190 compute hours/mo), serverless Postgres with branching, familiar SQL. Keeps costs at $0 until significant scale. | Yes | human |
| D004 |  | pricing | Pricing tiers and structure | Free (5 monitors, email), Pro $9/mo (50 monitors, Slack/Discord/webhooks, 30d retention), Team $29/mo (200 monitors, SMS/PagerDuty, 90d retention) | $9 sits below psychological $10 threshold, undercuts Healthchecks ($20) and Cronitor ($20+). Free tier at 5 monitors is more generous than competitors (1-3). 112 Pro customers = $1K MRR target. | Yes | collaborative |
| D005 |  | library | Auth provider | Clerk | Free up to 10K MAU, drop-in React components, JWT verification for API routes, webhook events for user lifecycle. Owner has existing experience. | Yes | human |
| D006 |  | library | Email provider for alerts | Resend | Free tier (100 emails/day, 3K/mo), simple API, React Email for templates. Sufficient for MVP alert volume. | Yes | collaborative |
| D007 |  | architecture | Miss detection scheduler architecture | Single setInterval polling loop at 30s granularity, DB-stored next_expected_at, stateless and restartable | Simplest correct solution. DB is the single source of truth — no in-memory state to lose on restart. 30s interval catches 1-minute grace periods. FOR UPDATE SKIP LOCKED enables future multi-process scaling. Single process on Railway handles 10K+ monitors. | Yes | agent |
| D008 |  | architecture | Monitor state machine | 4-state machine: new → up → grace → down → up. Grace period transitions are silent; alerts fire only on down and recovery. | Grace state prevents false alerts for late-but-acceptable pings. alert_sent_at column provides atomic deduplication. Advancing next_expected_at for stale down monitors prevents alert storms on restart. | Yes | agent |
| D009 |  | architecture | Ping endpoint response pattern | Respond before DB write (fire-and-forget). LRU cache for monitor slug lookup. In-memory write buffer with 3-attempt retry for DB failures. | Guarantees <200ms response regardless of Neon cold start latency. Monitor lookup from LRU cache is <1ms. Losing a ping write on DB failure is acceptable — the monitor would show as missed anyway. Industry standard (Healthchecks, Cronitor). | Yes | agent |
| D010 |  | architecture | Ping slug format | nanoid 21 chars (126-bit entropy, URL-safe alphabet) | Compact for curl one-liners, unguessable (brute force impractical even at 1000 req/s). UUID v4 is too long for CLI ergonomics. Slug serves as bearer token — no auth required on ping endpoint. | Yes | agent |
| D011 |  | architecture | Alert channel data model | User-level alert_channels table with JSONB config + monitor_alert_channels join table. Channels are reusable across monitors. | A user configures their Slack webhook once, then assigns it to multiple monitors. JSONB config handles heterogeneous channel types without per-type columns. miss_key unique index on alerts table provides idempotent deduplication. | Yes | agent |
| D012 |  | architecture | Frontend routing and real-time strategy | TanStack Router with pathless _auth layout guard, TanStack Query polling (60s list, 30s detail), dark-first theme, shadcn/ui (canary) for primitives | Polling matches cron job data cadence exactly — no WebSocket complexity for MVP. Pathless auth layout is the canonical TanStack Router pattern for protected routes. shadcn/ui gives accessible Radix primitives with Tailwind v4 support, owned in-repo. Dark-first because developer/devops user base. | Yes | agent |
| D013 |  | library | Neon driver mode | Neon serverless WebSocket pool for API routes, Neon HTTP driver as fallback option for ping path | WebSocket pool keeps connections warm across requests, dropping latency from 300-500ms (cold) to 20-60ms (warm). HTTP driver is stateless and faster from cold start but ping endpoint responds before DB write anyway, making driver choice less critical for that path. | Yes | agent |
